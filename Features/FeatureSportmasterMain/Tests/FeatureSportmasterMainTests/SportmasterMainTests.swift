import XCTest
@testable import SportmasterMain

final class SportmasterMainTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SportmasterMain().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
